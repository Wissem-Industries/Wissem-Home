when:
  - event: tag
    ref: refs/tags/v*

steps:
  - name: deployment-start
    image: oven/bun:1.4.2-alpine
    environment:
      GITHUB_PACKAGES_TOKEN:
        from_secret: github_packages_token
      DEPLOYMENT_ENVIRONMENT: production
      DEPLOYMENT_PRODUCTION: "true"
      DEPLOYMENT_TASK: deploy:production
    commands:
      - bun .woodpecker/github-deployment.mjs start

  - name: install
    image: oven/bun:1.4.2-alpine
    environment:
      GITHUB_PACKAGES_TOKEN:
        from_secret: github_packages_token
    commands:
      - printf '//npm.pkg.github.com/:_authToken=%s\n' "$${GITHUB_PACKAGES_TOKEN}" > /root/.npmrc
      - bun install --frozen-lockfile
      - rm -f /root/.npmrc

  - name: check
    image: oven/bun:1.4.2-alpine
    commands:
      - bun run check
    depends_on:
      - install
      - deployment-start

  - name: publish-image
    image: gcr.io/kaniko-project/executor:v1.23.2-debug
    environment:
      GITHUB_PACKAGES_TOKEN:
        from_secret: github_packages_token
    commands:
      - VERSION="$${CI_COMMIT_TAG#v}"
      - PACKAGE_VERSION=$(sed -n 's/.*"version"[[:space:]]*:[[:space:]]*"\([^"]*\)".*/\1/p' package.json | head -n 1)
      - test "$${VERSION}" = "$${PACKAGE_VERSION}"
      - mkdir -p /kaniko/.docker
      - AUTH=$(printf '%s:%s' 'WissemBad' "$${GITHUB_PACKAGES_TOKEN}" | base64 | tr -d '\n')
      - printf '{"auths":{"ghcr.io":{"auth":"%s"}}}\n' "$${AUTH}" > /kaniko/.docker/config.json
      - /kaniko/executor --context "$${CI_WORKSPACE}" --dockerfile Dockerfile.ci --build-arg GITHUB_PACKAGES_TOKEN="$${GITHUB_PACKAGES_TOKEN}" --destination "ghcr.io/wissem-industries/wissem-home:$${VERSION}" --destination ghcr.io/wissem-industries/wissem-home:latest
      - rm -f /kaniko/.docker/config.json
    depends_on:
      - check

  - name: deploy-production
    image: curlimages/curl:8.16.0
    environment:
      DOKPLOY_WEBHOOK_URL:
        from_secret: dokploy_webhook_url
    commands:
      - curl --fail --silent --show-error --retry 3 --retry-all-errors --max-time 30 --request POST "$${DOKPLOY_WEBHOOK_URL}"
    depends_on:
      - publish-image

  - name: deployment-success
    image: oven/bun:1.4.2-alpine
    environment:
      GITHUB_PACKAGES_TOKEN:
        from_secret: github_packages_token
      DEPLOYMENT_STATUS_DESCRIPTION: Mise à jour Dokploy déclenchée
      DEPLOYMENT_URL: https://www.wissem.pro
    commands:
      - bun .woodpecker/github-deployment.mjs success
    depends_on:
      - deploy-production

  - name: deployment-failure
    image: oven/bun:1.4.2-alpine
    environment:
      GITHUB_PACKAGES_TOKEN:
        from_secret: github_packages_token
      DEPLOYMENT_STATUS_DESCRIPTION: Échec du pipeline de publication
    commands:
      - bun .woodpecker/github-deployment.mjs failure
    when:
      - status: [failure]
